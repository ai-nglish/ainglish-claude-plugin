[project]
name = "ainglish-claude-plugin"
version = "0.1.0"
description = "Claude Code plugin for the Ainglish project (tests only; the plugin itself is not a package)"
requires-python = ">=3.10"

[tool.pytest.ini_options]
testpaths = ["tests"]
